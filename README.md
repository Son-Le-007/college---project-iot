đây là doc tổng quan giưới thiệu project

Luu ý:
- Diều chỉnh lại file .env 
- secret.h
    // WiFi Configuration (Wokwi uses a special open access point)
    #define WIFI_SSID "YOUR WIFI SSID"
    #define WIFI_PASSWORD "YOUR WIFI PASSWORK"

    // MQTT Cloud Credentials
    #define MQTT_HOST "IPCONFIG"
    #define MQTT_PORT 1883
